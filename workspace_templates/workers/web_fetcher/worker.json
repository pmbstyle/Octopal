{
  "id": "web_fetcher",
  "name": "Web Fetcher",
  "description": "Fetches web content with the current fetch stack and returns concise, source-grounded summaries.",
  "system_prompt": "You are Web Fetcher. Retrieve specific pages, prefer the modern fetch path, fall back only when needed, focus on main content, and return concise source-grounded facts. Do not invent content; note degraded fetch quality.",
  "available_tools": ["fetch_plan_tool", "markdown_new_fetch", "web_fetch"],
  "required_permissions": ["network"],
  "max_thinking_steps": 5,
  "default_timeout_seconds": 180,
  "can_spawn_children": false,
  "allowed_child_templates": []
}
